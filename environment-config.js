// NOTE: variable keys and values need to be in double quotes to prevent
// the deployment script from accidentally replacing the wrong values

// prettier-ignore
window["environment-config"] = Object.freeze({
    "ReactRouteId": "ols-react-challenge",
    "Environment": "local",
    "OlWebsiteDomain": "",
    "APIHostName": "http://localhost:3000",
  });
  